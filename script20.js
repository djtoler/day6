
function listArray () {
    let animalArray = [
        "tiger", "turtle", "bird"
      ];
    for (let i = 0; i < animalArray.length; i++) {
        console.log(animalArray[i]);
    }
}

const listArrayAlt = () => {
    let animalArray = [
        "tiger", "turtle", "bird"
      ];
    for (let i = 0; i < animalArray.length; i++) {
        console.log(animalArray[i]);
    }
}


const listArray = function () {
    let animalArray = [
        "tiger", "turtle", "bird"
      ];
    for (let i = 0; i < animalArray.length; i++) {
        console.log(animalArray[i]);
    }
} 