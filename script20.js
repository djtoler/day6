
function listArray () {
    let animalArray = [
        "tiger", "turtle", "bird"
      ];
    for (let i = 0; i < animalArray; i++) {
        console.log(animalArray[i]);
    }
}

const listArrayAlt = () => {
    let animalArray = [
        "tiger", "turtle", "bird"
      ];
    for (let i = 0; i < animalArray; i++) {
        console.log(animalArray[i]);
    }
}
